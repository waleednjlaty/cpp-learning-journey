#include <iostream>
#include <windows.h>
using namespace std;
struct Book
{

	int nam;
	string waleed;
};
int main()
{
	int count = 0;
	while (true)
	{
		system("cls");
		count++;

		cout << count << endl;
		Sleep(1000);
	}

	return 0;
}
