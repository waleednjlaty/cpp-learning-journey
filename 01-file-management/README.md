# File Management — SafeCopy CLI

A C++17 command-line project that copies a file to a destination directory while practicing filesystem operations, validation, exception handling, and RAII.

## Build

```bash
g++ -std=c++17 src/main.cpp -o safecopy
./safecopy
```

The source code is in `src/`.
